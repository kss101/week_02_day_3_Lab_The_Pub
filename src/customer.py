class Customer:
    def __init__( self, name, wallet ):
        self.name = name
        self.wallet = wallet


    def buy_item(self, item):
        self.drinks += (pub.drink(item))


        # drink_order = pub.drink(item)
        # pull drink from pub list
        # add drink to customer.drinks
        # remove money from customer.wallet
        #  add money to pub.till
        print(drink_order)